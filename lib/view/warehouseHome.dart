import 'package:flutter/material.dart';

class WareHouseHome extends StatelessWidget {
  const WareHouseHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
